{
  "name": "x-thread-clean-capture-firefox",
  "version": "1.0.13",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint:extension": "web-ext lint --source-dir . --ignore-files test package.json package-lock.json"
  },
  "devDependencies": {
    "jsdom": "^27.0.0",
    "web-ext": "^9.1.0"
  }
}
